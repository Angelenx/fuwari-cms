# Copy to .dev.vars for local development. Do not commit .dev.vars.
# Production: `pnpm exec wrangler secret put SESSION_SECRET` (never put the value in wrangler.jsonc).
SESSION_SECRET="replace-with-a-long-random-string"

# First visit to /admin/login creates the admin password (username is always admin).
